server.port=8080
spring.application.name=music-player-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/music_player?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Yara_182433_6244
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# File Storage
music.storage.path=backend/storage/media

# Max file upload size
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# CORS Configuration
cors.allowed.origins=http://localhost:*

# Security (disable for simplicity, can be enabled later)
spring.security.user.name=admin
spring.security.user.password=admin123